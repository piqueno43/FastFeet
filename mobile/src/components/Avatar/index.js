// import React from 'react';
// import { Image } from 'react-native';

// import BadgeName from '~/components/BadgeName';

// export default function Avatar({ name, size }) {
//   return (
//     <>
//       {avatar ? (
//         <Image source={{ uri: url }} />
//       ) : (
//         <BadgeName name={name} size={size} />
//       )}
//       }
//     </>
//   );
// }
